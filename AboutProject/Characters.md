# کاراکترها و آرتشان

**نخِ قرمزِ پوسیده** · یونیتی (2D / ویژوال ناول)

فایل‌های واقعی در `AboutProject/Characters/<Name>/` هستند. در پروژه‌ی یونیتی جایشان `Assets/Images/Characters/<Name>/` است.

---

## ۱. مشخصاتِ فنی

* **اندازه‌ی بوم:** `1200 × 2400 px` — همه‌ی اسپرایت‌ها، بدونِ استثنا. فرمت `PNG` با آلفا.
* **قد:** هارو ۱۷۳ سانتی‌متر · یوآ ۱۶۵ سانتی‌متر.
* **اختلافِ ۸ سانتی داخلِ خودِ تصویر پخته شده** — یوآ حاشیه‌ی بالای بوم دارد و کلِ ۲۴۰۰ پیکسل را پر نمی‌کند.
* **در موتور هیچ مقیاسِ دستی‌ای اعمال نمی‌شود.** `Transform.localScale = (1, 1, 1)` برای هر دو.

### جای ایستادن

عددها در `_C#/Narrative/StageSettings.cs` (`Placement`) و در `StageSettings.asset` هستند. مختصاتِ **world**، نه canvas.

| | X | Y | Scale |
|---|---|---|---|
| یوآ | `-4.55` | `-1.50` | `0.48` |
| هارو | `4.70` | `-1.50` | `0.50` |
| یوآ (۹ ساله) | `-4.55` | `-1.50` | `0.335` |
| هارو (۹ ساله) | `4.70` | `-1.50` | `0.350` |

> بازی **فقط** `StageSettings.asset` را می‌خواند. Marker های داخلِ Scene خوانده نمی‌شوند.
> جزئیات و تاریخچه‌ی خرابی‌هایش: `Guide-Engine.md` بخش «جای کاراکترها».

---

## ۲. گوینده‌ها — `Speaker`

`_C#/Narrative/CharacterArt.cs`. عددها ثابت‌اند و داخلِ asset ها ذخیره می‌شوند — **هیچ‌وقت وسطِ enum اضافه نکن.**

| # | `Speaker` | پلاکِ اسم | اسپرایت | توضیح |
|---|---|---|---|---|
| ۰ | `Narrator` | ندارد | ندارد | توصیف، فضاسازی، کنشِ فیزیکی |
| ۱ | `Yua` | یوآ / ユア / Yua | دارد | شخصیتِ بازیکن، و کسی که بازی را می‌گرداند |
| ۲ | `Haru` | هارو / ハル / Haru | دارد | |
| ۳ | `Player` | دارد | ندارد | خطاب به کسی که کنترلر دستش است. از پرده‌ی دوم |
| ۴ | `YuaChild` | دارد | **هنوز کشیده نشده** | یوآی نُه‌ساله، پرده‌ی ششم |
| ۵ | `HaruChild` | دارد | **هنوز کشیده نشده** | هاروی نُه‌ساله، پرده‌ی ششم |
| ۶ | `Classmate` | دارد | **عمداً ندارد** | دختری از کلاسشان. صدایی از کنارِ صحنه |

### چرا `Classmate` بدن ندارد

او فقط برای **یک کار** وجود دارد: در ژاپنی `ぴ` اسلنگِ واقعیِ صمیمیت است و بازیکنِ ژاپنی بارش را بارِ اول می‌گیرد؛ بازیکنِ فارسی و انگلیسی فقط یک لقب می‌شنود. پس کسی از بیرونِ آن دو نفر باید **یک بار** بلند به آن اشاره کند و جوابِ «به تو ربطی ندارد» بگیرد — و پسوند همان بارِ زبانی را بگیرد که در یکی از سه زبان از اول داشت.

بدون اسپرایت، `VisualNovelStage` خودش کارِ درست را می‌کند: هیچ‌کدام از دو جایگاه او را ندارند، پس یوآ و هارو هر دو یک قدم عقب می‌روند — دقیقاً همان چیزی که در یک راهرو وقتی نفرِ سوم حرف می‌زند اتفاق می‌افتد.

> ✅ **برطرف شد.** در بازنویسیِ پرده‌ی اول، «هاروپی» در فریمِ **بلافاصله قبلِ** سؤالِ همکلاسی گفته می‌شود، در حضورش و با صدای عادی — «هاروپی، سیخ رو بده من، کجش گرفتی» — و بعد «...ببخشید، الآن چی صداش کردی؟». قبلاً سؤال پرسیده می‌شد بدونِ اینکه چیزی برای شنیدن وجود داشته باشد.

---

## ۳. حالت‌های چهره — `Portrait`

enum ها روی **احساس** نام‌گذاری شده‌اند، نه روی فایل، چون دو کاراکتر یک احساس را جور دیگری املا می‌کنند: یوآ وقتی خجالت می‌کشد سرش را **پایین** می‌اندازد و هارو **رویش را برمی‌گرداند**، عصبانیتِ او یک **glare** است و مالِ هارو یک **frown**. اسکریپت نباید این را بداند.

### چهره‌های پایه

| # | `Portrait` | یوآ | هارو |
|---|---|---|---|
| ۰ | `Unchanged` | — هرچه روی صفحه است می‌ماند — | |
| ۱ | `Neutral` | `YuaNeutralGentleSmile` | `HaruNeutralGentleSmile` |
| ۲ | `Joyful` | `YuaJoyfulHappyLaugh` | `HaruJoyfulHappyLaugh` |
| ۳ | `Shy` | `YuaShyBlushingLookDown` | `HaruShyBlushingLookAway` |
| ۴ | `Sad` | `YuaSadImploringTearful` | `HaruSadImploringTearful` |
| ۵ | `Angry` | `YuaAnnoyedAngryGlare` | `HaruSeriousAngryFrown` |
| ۶ | `DeadEyes` | `YuaDeadEyesPokerFace` | `HaruDeadEyesPokerFace` |
| ۷ | `Manic` | `YuaInsaneManicSmile` | `HaruInsaneManicSmile` |
| ۸ | `Crying` | `YuaSorrowfulCryingTears` | `HaruSorrowfulCryingTears` |
| ۹ | `Injured` | — ندارد، به `Neutral` می‌افتد — | `HaruInjuredKneeGrimace` |

### بنتو — ده فریم، یک کنشِ مشترک

یک اسمِ `Portrait`، دو نقاشیِ متفاوت از **یک لحظه**. اسکریپت یک بار `LunchFirstLift` می‌گوید و هر دو را یعنی.

| # | `Portrait` | یوآ | هارو |
|---|---|---|---|
| ۱۰ | `LunchOpen` | `YuaBento01HoldClosedBox` | — |
| ۱۱ | `LunchOffer` | `YuaBento02ShowFullFood` | `HaruBento01HoldEmptyLid` |
| ۱۲ | `LunchShared` | `YuaBento03SharedMostlyEmpty` | `HaruBento02FoodReceived` |
| ۱۳ | `LunchFirstLift` | `YuaBento04LiftFirstSushi` | `HaruBento03LiftFirstOctopus` |
| ۱۴ | `LunchFirstBite` | `YuaBento05SavorFirstSushi` | `HaruBento04SavorFirstOctopus` |
| ۱۵ | `LunchSecondLift` | `YuaBento06LiftLastSushi` | `HaruBento05LiftFirstSushi` |
| ۱۶ | `LunchSecondBite` | `YuaBento07SavorLastSushi` | `HaruBento06SavorFirstSushi` |
| ۱۷ | `LunchThirdLift` | `YuaBento08LiftLastOctopus` | `HaruBento07LiftSecondOctopus` |
| ۱۸ | `LunchThirdBite` | `YuaBento09SavorLastOctopus` | `HaruBento08SavorSecondOctopus` |
| ۱۹ | `LunchFinished` | `YuaBento10ClosedFinishedSmile` | — به `Neutral` می‌افتد — |

**هارو یک تصویر عقب‌تر است، در تمامِ توالی.** یوآ باید جعبه را باز کند تا او چیزی برای نگه‌داشتن داشته باشد، پس اولین نقاشیِ او روی دومینِ او می‌افتد؛ و وقتی جعبه بسته می‌شود، دستِ هارو خالی است و تصویری ندارد — که همان چهره‌ی خنثایش است.

> **عدد در دیالوگ = عدد در فریم.** اگر متن می‌گوید «شش تا سوشی، چهار تا سوسیس»، توالی باید ده بار برداشتن داشته باشد — یا عدد گفته نشود.
> محتوای جعبه در آرت: یوآ ۶ سوشی + ۴ سوسیسِ اختاپوسی؛ ۴ سوشی + ۳ سوسیس سهمِ هارو می‌شود.

### نوشیدنی — سه فریم

| # | `Portrait` | یوآ (بابل‌تی) | هارو (ماچا) |
|---|---|---|---|
| ۲۰ | `DrinkFull` | `YuaBobaSipFullCup` | `HaruMatchaHoldFullCup` |
| ۲۱ | `DrinkReluctant` | `YuaBobaHoldEmptyCup` | `HaruMatchaSipReluctant` |
| ۲۲ | `DrinkFinished` | `YuaPeacefulClosedEyesSmile` | `HaruMatchaHoldEmptyCup` |

مالِ او تمام شده و هارو تازه دارد ماچایی را که دوست ندارد شروع می‌کند. آخرش هر دو راضی‌اند، به دو دلیلِ متفاوت.

### رجیسترِ میانه — هشت چهره‌ی افزوده

بین «لبخندِ معمولی» و «چهره‌های سنگین» هیچ‌چیز نبود، و شمارشِ واقعی می‌گفت **۹۰٪ پرده‌ی اول سه حالت است**. این هشت تا آن وسط‌اند. برخلافِ چهره‌های امضا، **جیره ندارند.**

توصیف‌ها از روی خودِ تصویرها نوشته شده، نه از روی اسمِ فایل.

| # | `Portrait` | یوآ | هارو | چه چیزی واقعاً کشیده شده |
|---|---|---|---|---|
| ۲۳ | `Pout` | `YuaPoutTsundere` | ← `SeriousAngryFrown` | **یوآ:** دست‌به‌سینه، رو به جلو، دهانِ صاف و کمی آویزان. اخم نیست — نپذیرفتن است. برای هر بحثِ بی‌اهمیتی که می‌بازد.<br>**هارو:** ندارد و لازم ندارد. او قهر نمی‌کند، کوتاه می‌آید. |
| ۲۴ | `Smug` | `YuaSmugMischievousSmile` | ← `JoyfulHappyLaugh` | **یوآ:** سرِ کمی کج، لبخندِ بسته و راضی، دست‌ها پایین. **مهم‌ترینِ این هشت تا**: چهره‌ی «پاداش» — محبتِ کوچک، درست بعد از اینکه چیزی را که می‌خواست گرفت. تا حالا `Joyful` جایش را می‌گرفت و بارِ کاملاً متفاوتی دارد. |
| ۲۵ | `Surprised` | `YuaSurprisedTakenAback` | `HaruSurprisedTakenAback` | هر دو: چشم‌های گرد، دهانِ نیمه‌باز، یک دست بالا آمده نزدیکِ سینه. بدونِ این، هیچ شوخی‌ای جواب نمی‌دهد. |
| ۲۶ | `Bored` | `YuaBoredUnamused` | `HaruDeadpanFlat` | هر دو: **چشم‌ها بسته، دست‌به‌سینه**، دهانِ خط. ⚠️ **با `DeadEyes` قاطی نشود** — این یکی یعنی «بامزه نبود»، آن یکی یعنی کسی خانه نیست. آن‌قدر شبیه‌اند که استفاده‌ی این در صحنه‌ی قبل از یک `DeadEyes`، آن `DeadEyes` را خرج می‌کند. |
| ۲۷ | `Thinking` | `YuaThinkingLookingUp` | ← `NeutralGentleSmile` | **یوآ:** یک دست روی سینه، دستِ دیگر بالا با انگشتِ اشاره. **رو به بالا نگاه نمی‌کند** — اسمِ فایل گمراه‌کننده است و همان می‌ماند چون فایل همان است. اشاره کردن بهتر از نگاه به بالاست: نگاه به بالا یعنی دارد فکر می‌کند، اشاره یعنی **تصمیمش را گرفته**. |
| ۲۸ | `StrainedSmile` | ← `SadImploringTearful` | `HaruStrainedPainfulSmile` | **هارو:** یک دست کنارِ گردن، لبخند می‌زند و لبخند کار نمی‌کند. **کلِ شخصیتش در یک تصویر.** برای هر خطی که پایش نگهش داشته و چیزِ دیگری می‌گوید. |
| ۲۹ | `Sheepish` | ← `ShyBlushingLookDown` | `HaruSheepishHandBehindHead` | **هارو:** دست پشتِ سر، لبخندِ دستپاچه، چشم‌ها باریک. چهره‌ی «ببخشید» و مچ‌گیری‌شدن. `Shy` فعلی رو برمی‌گرداند و برای عذرخواهی کار نمی‌کند. |
| ۳۰ | `CalmSerious` | ← `NeutralGentleSmile` | `HaruCalmSerious` | **هارو:** دست روی کمر، لبخندِ کوچک، نگاهِ مستقیم. راحت و مطمئن — نه عصبانی. برای همان یک بحثِ بی‌اهمیتی که در هر صحنه می‌برد؛ `Angry` برایش خیلی زیاد است و `Neutral` شبیهِ کسی نیست که حق با اوست. |

### سرما و لوازمِ زمستان — شش حالتِ افزوده

پرده‌ی دوم از اکتبر تا دسامبر می‌گذرد و بازی هیچ راهی نداشت که سرما را نشان بدهد: همه‌ی اسپرایت‌ها دست‌ها پایین، انگار هنوز سپتامبر است. این شش تا آن را حل می‌کنند. **سه تای آخر «لوازم» حساب می‌شوند** (`IsPropPose`) و بچه‌ها ندارندشان.

| # | `Portrait` | یوآ | هارو | چه چیزی واقعاً کشیده شده |
|---|---|---|---|---|
| ۳۱ | `ColdHug` | `YuaArmsHuggingCold` | ← `HandsInPockets` | **یوآ:** دست‌ها روی سینه بغلِ خودش، همان لبخندِ آرام. در نوامبر و دسامبر توی پیراهنِ تابستانی است و قبولش نمی‌کند — شوخی و شخصیت، هر دو. **هارو:** دستش را می‌کند تو جیبش، که همان معنی است با ژستِ خودش. |
| ۳۲ | `Counting` | `YuaCountingFingers` | ← `NeutralGentleSmile` | **یوآ:** یک دست بالا با **هر پنج انگشتِ باز**، دستِ دیگر روی کمر. آخرین تصویرِ شمارش. |
| ۳۷ | `CountingOne` | `YuaCountingFingers01` | ← | یک انگشت |
| ۳۸ | `CountingTwo` | `YuaCountingFingers02` | ← | دو انگشت |
| ۳۹ | `CountingThree` | `YuaCountingFingers03` | ← | سه انگشت |
| ۴۰ | `CountingFour` | `YuaCountingFingers04` | ← | چهار انگشت |

> **چرا پنج نقاشی برای پنج عدد.** تا وقتی فقط `YuaCountingFingers` وجود داشت، صحنه‌ی زمینِ بازی «یک، دو، سه، چهار» را روی دستی می‌شمرد که از اول **پنج** انگشتش باز بود — یعنی عددی که کلِ پرده بی‌صدا به سمتش می‌رود، روی تصویری می‌رسید که بازیکن چهار کادر بود داشت نگاهش می‌کرد.
>
> حالا دست پر می‌شود، و روی «پنج» پر است و **یک ثانیه و نیم بیشتر از چیزی که یک شمارشِ تمام‌شده لازم دارد بالا می‌ماند**. آن مکث، ◆ دلهره‌ی هشتمِ پرده‌ی اول است: نه یک چهره، نه یک کلمه. `Sad` و `DeadEyes` خرج نمی‌شوند.
| ۳۳ | `HoldCan` | `YuaHoldWarmCan` | `HaruHoldWarmCan` | هر دو: دو دست دورِ یک قوطی. بدونِ بخار، پس برای شبِ سپتامبر (دو قوطی از دستگاه) هم کار می‌کند و هم برای ۵ نوامبر. |
| ۳۴ | `WarmCup` | `YuaWarmCupHoldUntouched` | `HaruWarmCupHoldFull` | دو دست دورِ فنجانِ داغِ پر. پاسخِ زمستانیِ `DrinkFull` — آن یکی ماچای یخیِ نی‌دار است و مالِ سپتامبر. |
| ۳۵ | `WarmCupEmpty` | ← `WarmCupHoldUntouched` | `HaruWarmCupHoldEmpty` | **مالِ هارو.** یوآ به `WarmCup` می‌افتد و این دقیق است، نه سازش: در صحنه‌ای که این برایش وجود دارد، فنجانش تو دستش سرد می‌شود و اصلاً بهش دست نمی‌زند. **همان نقاشی، نود فریم بعد** — و این کلِ آن صحنه است. |
| ۳۶ | `Pockets` | ← `NeutralGentleSmile` | `HaruHandsInPockets` | **هارو:** هر دو دست تو جیب. کاری که به‌جای نگه‌داشتنِ چیزِ گرمی که تازه بخشیده می‌کند. راوی یک بار، در پرده‌ی دوم، بلند می‌گویدش. **یوآ:** پیراهنش جیب ندارد. |
| ۴۱ | `OfferDrink` | ← `NeutralGentleSmile` | `HaruShylyOffersDrink` | **هارو:** قوطی رو با دستِ دراز از پهلو گرفته سمتِ یوآ و **صورتش رو برگردونده**. یک نقاشی برای کلِ پله‌ی چهارمِ پرده‌ی دوم. سه پیش‌نویسِ این لحظه روی مبلمانی نوشته شده بود که وجود نداشت — اول ژاکت، بعد دیوار — و چهارمی فقط همین تصویره. **یوآ:** نسخه‌ای ندارد و لازم هم ندارد؛ او هیچ‌وقت طرفِ تعارف‌کننده نیست. |

> ⚠️ **اسمِ فایلِ `OfferDrink` یک فاصله‌ی اضافه دارد.** فایلِ داخلِ پروژه `HaruShylyOffersDrink .png` است — **یک فاصله قبل از نقطه**. یونیتی اسمِ asset را «`HaruShylyOffersDrink `» می‌خواند و کد دنبالِ «`HaruShylyOffersDrink`» می‌گردد، پس هیچ‌وقت پیدایش نمی‌کند و به‌جایش یک سایه نشان می‌دهد.
>
> **کد با اسمِ درست نوشته شده. فایل باید rename شود** — در یونیتی و در گیت‌هاب، هر دو.

`←` یعنی آن شخصیت تصویرِ خودش را ندارد و کد به چهره‌ی نوشته‌شده می‌افتد. عمدی است، نه کمبود.

---

## ۴. کاراکترهای بچه — پرده‌ی ششم

اسمِ فایل‌ها **از الآن قفل است**: دقیقاً اسمِ بزرگسال با `Child` بعد از اسمِ کاراکتر.

```
YuaChildNeutralGentleSmile.png     HaruChildNeutralGentleSmile.png
YuaChildJoyfulHappyLaugh.png       HaruChildJoyfulHappyLaugh.png
YuaChildShyBlushingLookDown.png    HaruChildShyBlushingLookAway.png
YuaChildSadImploringTearful.png    HaruChildSadImploringTearful.png
YuaChildAnnoyedAngryGlare.png      HaruChildSeriousAngryFrown.png
YuaChildDeadEyesPokerFace.png      HaruChildDeadEyesPokerFace.png
YuaChildSorrowfulCryingTears.png   HaruChildSorrowfulCryingTears.png
                                   HaruChildInjuredKneeGrimace.png
```

هشت چهره‌ی رجیسترِ میانه برای بچه‌ها **لازم نیست**. کد اجازه‌شان را می‌دهد (مثلاً `YuaChildPoutTsundere`)، ولی پرده‌ی ششم کوتاه است و تمامش در حالتِ فیلم می‌گذرد.

**هیچ‌کدام هنوز کشیده نشده‌اند** و پرده‌ی ششم بدونشان هم اجرا می‌شود (اسپرایتِ غایب = یک سایه). همین باعث می‌شود بشود پرده را امروز نوشت و قضاوت کرد.

بچه‌ها **لوازم ندارند** — نه بنتو، نه نوشیدنی. `WithoutProps()` در کد این را تضمین می‌کند: فلش‌بک یک حیاطِ مدرسه و یک موتورخانه است.

---

## ۵. لباس — و یک تناقضِ مهم

**هیچ‌کدامِ این دو نفر لباسِ مدرسه نمی‌پوشند.** هر ۳۴ اسپرایتِ یوآ و هر ۳۱ اسپرایتِ هارو یک دستِ لباس دارند و همان است:

| | چه چیزی کشیده شده |
|---|---|
| **یوآ** | پیراهنِ چین‌دارِ صورتی و آبی روی بلوزِ سفید، کمربندِ آبی، جورابِ توری، کفشِ پاپیون‌دار، گل در مو، چوکرِ صورتی. **و نخِ قرمز دورِ مچ‌هایش** — در چهره‌های سنگین پررنگ‌تر، در چهره‌های روزمره نازک‌تر |
| **هارو** | ژاکتِ کرمِ بافت، پیراهنِ آبیِ روشن، شلوارِ خاکستری، کتانیِ سفید-آبی |

> ✅ **برطرف شد در پرده‌های ۱ و ۲.** شوخیِ لباسِ فرم کاملاً حذف شد و جایش را پیراهنِ خودش گرفت — دخترِ نگرانِ پیراهنی که خودش انتخاب کرده، از دخترِ نگرانِ یونیفرمی که مدرسه انتخاب کرده **بیشتر** درباره‌ی خودش می‌گوید. کلمه‌ی «فرم» در هیچ دیالوگی از این دو پرده نیست (شمرده شد: صفر).
>
> ⚠️ **دو جا هنوز مانده:** جدولِ تداومِ دستورنامه (بخشِ ۲۳) و نمونه‌ی کالیبراسیونش (بخشِ ۲۶) هنوز روی شوخیِ یونیفرم بنا شده‌اند. تا وقتی درست نشوند، هرکسی که از روی دستورنامه دیالوگ بنویسد دوباره همان باگ را می‌سازد.
>
> **قانون: آرت مرجع است.** اگر دیالوگ به چیزی اشاره می‌کند که در اسپرایت نیست، دیالوگ عوض می‌شود.
>
> ⚠️ **همین قانون یک بار دیگر جلوی یک صحنه را گرفت.** پیش‌نویسی از ۵ نوامبر داشت که هارو ژاکتش را درمی‌آورد و می‌انداخت روی شانه‌ی یوآ — تصویرِ بهتر، و غیرممکن: هر شصت اسپرایت او را با ژاکت نشان می‌دهند. جایش قوطیِ داغ آمد، که همان کار را با چیزی می‌کند که از قبل روی صحنه است.

---

## ۵.۵ قانونِ تازه — هیچ چهره‌ای یخ نمی‌زند

یک پلی‌تست نشان داد در دو پرده **۳۰ جا** بود که یک نقاشی هفت کادر یا بیشتر روی صفحه می‌ماند. بدترینشان: یوآ **نوزده کادر** دست‌به‌سینه در کافه، و هارو **دوازده کادر** روی همان لبخندِ دردناک — وسطِ سنگین‌ترین صحنه‌ی بازی.

سه ریشه داشت و هر سه بسته شد:

| ریشه | چه بود | تعمیر |
|---|---|---|
| **شاخه‌های انتخاب** | داخلِ یک راه فقط `Say` مجاز است، پس هیچ راهی نمی‌تواند کسی را تکان بدهد؛ هر دو نفر با همان چهره‌ای بیرون می‌آمدند که قبلش داشتند | بعد از **هر** `DecideIdly` شاخه‌دار یک `Cel(...)` روی کادرِ اتصال که تصویرِ هر دو را صریح می‌گوید. ۱۳ جا |
| **تک‌گویی** | `InnerVoice` همیشه `Portrait.Unchanged` بود، پس هر مونولوگ چهره‌ی خطِ قبلی را ارث می‌برد — اولین دلهره‌ی پرده‌ی اول با پوزخند و دست‌به‌کمر اجرا می‌شد | `InnerVoice(Portrait.X, …)` اضافه شد |
| **بلوکِ راوی** | سه چهار `Narrate` پشتِ سرِ هم یعنی سه چهار کادر که هیچ‌کس حرف نمی‌زند و هیچ‌کس تکان نمی‌خورد | `Cel(...)` وسطشان — مثلاً وقتی معلم وارد می‌شود، هر دو صاف می‌نشینند |

**سقفِ عملی:**

* **چهره‌های واکنشی** (`Surprised`، `Pout`، `Smug`، `Bored`، `Sheepish`، `StrainedSmile`، `Shy`) — حداکثر **۴ کادر**.
* **چهره‌های استراحت** (`Neutral`، `CalmSerious`) — حداکثر **۸ کادر**.
* **ژست‌های بدن** (`ColdHug`، `HoldCan`، `Pockets`) بیشتر می‌مانند، ولی نه بدونِ شکست. نوزده کادرِ بی‌حرکت «سکون» نیست، «باگ» است.

نتیجه: از ۳۰ مورد به **۷** رسید، و هیچ‌کدام از آن هفت تا بیشتر از ۹ کادر نیست.

---

## ۶. جیره‌ی چهره — قانونِ دستورنامه

> **یک چهره برای همه‌ی تک‌گویی‌ها یعنی ترسناک‌ترین تصویرِ پروژه تبدیل می‌شود به کاغذدیواری.**

| چهره | جیره در هر پرده |
|---|---|
| `DeadEyes` | **یک بار** — و برای همان یک حالتی که مالِ اوست |
| `Sad` (چهره‌ی نگران) | **یک بار** — برای بلندترین لحظه |
| `Crying` | فقط جایی که هیچ‌کس در داستان اجازه‌ی دیدنش را ندارد |
| `Joyful` | برای انکار و برای لذت — نه برای شادیِ معمولی در تک‌گویی |
| `Manic` | پرده‌ی پنجم به بعد |

**یک چهره‌ی امضا که دو بار بیاید، نصف می‌شود. سه بار بیاید، صفر می‌شود.**

> **نکته:** چهره‌های کمدی و مکالمه (`Pout`، `Smug`، `Surprised`، `Bored`، `Thinking`، `StrainedSmile`، `Sheepish`، `CalmSerious`) و حالت‌های بدن و لوازم (`ColdHug`، `Counting`، `HoldCan`، `WarmCup`، `WarmCupEmpty`، `Pockets`) **جیره‌ی محدود ندارند** و می‌توانند آزادانه در صحنه‌های روزمره و بحث‌های بامزه استفاده شوند تا ریتم بازی از یکنواختی خارج شود.
>
> ولی حالت‌های لوازم یک قانونِ دیگر دارند: **`Portrait.Unchanged` برای خطِ بعدی**، وگرنه قوطی یا فنجان با اولین تغییرِ چهره از دست می‌رود. کلِ ترفندِ صحنه‌ی کافه‌ی دسامبر همین است.

استثنا: `DeadEyes` یوآ در لحظه‌ی اورراید کردنِ گزینه‌ی آبی جیره ندارد — آن یک مکانیک است، نه یک انتخابِ کارگردانی. بعد از اورراید، صورتش **پوکر می‌ماند** و عمداً همان‌جا رها می‌شود؛ بیتِ بعدی حتماً باید یک `Portrait` بدهد.

---

## ۷. چه چیزی هنوز کم است

| | |
|---|---|
| **بچه‌ها** | ۱۵ اسپرایت، پرده‌ی ششم. اسم‌ها بالا قفل شده‌اند |
| **DLC ها** | میزوکی (水月) · کایتو (海斗) · میناتو (湊) · یوما (結真) · دایی · دکتر |
| **بزرگسال‌ها** | چیزی کم نیست. **۳۴ چهره‌ی یوآ** و **۳۱ چهره‌ی هارو** کلِ پرده‌های ۱ تا ۵ را پوشش می‌دهند. شمرده شد: هر ۴۱ مقدارِ `Portrait` برای **هر دو** کاراکتر به یک فایلِ موجود می‌رسد — به‌جز `OfferDrink` که فایلش فاصله‌ی اضافه دارد |

---

## ۸. اضافه کردنِ کاراکترِ جدید

پنج جای کد باید عوض شود و صحنه فقط **دو** جایگاه دارد. مراحلِ دقیق: `Guide-Engine.md` بخش «اضافه کردن یک کاراکتر جدید».

فهرستِ کاراکترهای غایب در بخشِ ۷ است.
